# 抢票实战计划

> 编写日期：2026-04-25 · 修订：v2

## 〇、先讲清楚定位

这个工具**不是**"按钮一按帮你抢首发顶票"。猫眼/大麦的下单接口都有 JS 混淆签名（`mtop-sign`、`sing`），全自动下单需要逆向算法、平台一改月月废，普通人玩不动。

这个工具能稳定做到的有两件事：
1. **7×24 监控四平台库存**，回流票一出现 1 秒内推送到你 Telegram。
2. **开抢瞬间手机端按预设流程自动点 5 步**（不是 50 次/秒乱点），把人类反应延迟降到 0。

**理解定位之后**，下面分两类用户给路径。

---

## 一、用户分型

| 类型 | 特征 | 主战场 | 命中率 |
|---|---|---|---|
| **回流党** | 错过首发的多数人；演出还没开 | A 路监控 | 30–60% |
| **首发党** | 真正首发开抢那 60 秒 | A + B 路并用 | 5–15% |

**重要事实**：演唱会票 70% 以上的"普通人最终拿到票"是通过**回流**而非首发。所以默认推荐路径是**先做好 A 路监控**，再考虑 B 路。

---

## 二、SOP-1：回流党（推荐起手）

### 准备（一次性，30 分钟）

1. 一台能 7×24 在线的设备（旧手机 / 99 元/年 ECS / 树莓派均可）
2. 一个 Telegram bot（5 分钟搞定，下面教）

### Telegram bot 配置

```
1. 手机 Telegram 找 @BotFather
2. /newbot → 给 bot 起名 → 拿到 bot_token（一串 123456:ABC...）
3. 找你刚创建的 bot，按"开始"或随便发一条消息
4. 浏览器访问：
   https://api.telegram.org/bot<BOT_TOKEN>/getUpdates
5. 找到 message.chat.id 那个数字，就是 chat_id
```

### 启动监控

```bash
cd TicketMonitoring-master
pip install -r requirements.txt
python3 setup_wizard.py     # 交互式向导，自己粘 URL 即可
python3 start.py            # 自检通过后开始监控
```

向导会问你 4 件事：演出 URL（自动解析 show_id）、备注名、Telegram 凭据、token。

### 跑起来后会看到什么

```
INFO  自检通过：猫眼 周杰伦武汉站 当前可买 0 档
INFO  ✅ Telegram 通道就绪
INFO  💓 心跳报告
        🟢运行 猫眼 周杰伦武汉站 | 检查 312 成功 312 失败 0 | 回流告警 0 次 | 最近成功 14:23:11
INFO  🎫 回流啦！猫眼《周杰伦武汉站》共 2 档可买，立即下单：14:25:33
```

收到推送 → **立刻打开猫眼 App 提交订单**（30 秒内）。回流票**抢手**，迟一分钟就被刷走。

### 持续运营

- token 30 天会过期；启动自检失败会立刻告警，重新抓即可
- 演出**前 24 小时 + 演出当天**回流最多，那天关注度拉满
- 心跳 5 分钟一报，监控挂了能立刻发现

---

## 三、SOP-2：首发党（高难度，可选加挂 B 路）

### 假设你已经做完 SOP-1

A 路监控会自动加速：**deadline 前 1 小时内 15s/次，10 分钟内 5s/次，1 分钟内 1s/次**——开抢瞬间是接口能压的最大节奏。所以即便不开 B 路，A 路在开抢瞬间已经在帮你跑了。

### B 路：手机端 uiautomator2 加挂

```bash
# 在你的 Android 手机上（Termux 内）
cd ~/TicketRush/mobile_termux
cp config.json.example config.json
nano config.json   # 改 target_date 和 target_tickets
termux-wake-lock
python termux_grabber.py
```

`config.json` 里那个 `click_rate_per_sec` **不要超过 15**，2025 年起猫眼对 >15/秒有风控封号。

### T-60s 到 T+5min 剧本

| 时刻 | A 路（云端监控） | B 路（手机端） | 你（人）|
|---|---|---|---|
| T-60s | 1s 间隔轮询中 | 已停在演出详情页 | 把 4 个平台 App 都打开 |
| T-10s | 持续轮询 | 持续监听按钮 | 不要手动刷新 |
| T+0 | 收到推送（如果是回流模式） | 自动点"立即预订" → 选场次 → 选票档 → 提交订单 | 人手在另一台手机也点提交 |
| T+30s | 继续监控（防止失败重新放票）| 进入支付页 → 停 | **立即手动支付** |
| T+5min | 没拿到 → 切回回流模式 | 退出 | 准备守 7 天 |

### 前置必备

- 4 个平台都已实名 + 收货地址 + **免密支付**
- 目标演出每个平台都点了"想看"/"提醒"
- NTP 校时：`sudo ntpdate ntp.aliyun.com`，手机开"自动设置时间"
- 至少 2 个独立账号（自己 + 家人），每个走不同 IP

---

## 四、产品边界

### 不能做（且不应做）的

- **全自动支付**：留给人类是合规底线，也是防止技术故障"误下单几千块"的保险
- **批量代抢**：会被平台封号 + 涉嫌违法
- **绕过实名**：人脸/身份证就是给真人用的

### 路线图（按价值排序）

- ✅ **已交付**：A 路监控 + 启动自检 + 心跳 + Telegram 推送 + 自适应频率 + 失败回退
- ✅ **已交付**：B 路 Termux 单文件脚本 + 配置外置 + 风控友好默认
- ✅ **已交付**：交互式 setup 向导 + URL→show_id 解析 + 抓包指南
- 🟡 **待评估**：大麦 cookie 自动刷新（当前 `Monitor_DM.py` 内嵌的 cookie 仍需手填）
- 🟡 **待评估**：监控接入 Server 酱/WxPusher（不用 Telegram 的人替代方案）
- 🔴 **故意不做**：自动下单、自动支付、绕过签名

---

## 五、最常踩的坑

| 现象 | 真实原因 | 怎么发现 |
|---|---|---|
| 监控跑了一天没动静 | token 过期，被 silent fail | 现在 monitor 会抛错，启动自检 + 心跳能秒发现 |
| 收到推送但点开已售罄 | 回流票流速快，30 秒就被抢光 | 把 Telegram 推送音设为最高优先级 |
| 大麦监控一直报错 | `Monitor_DM.py` 内嵌的 cookie 是作者老账号的，必死 | 见 `抓包指南.md` 第 2 节 |
| 手机脚本不点 | 屏幕休眠+电池优化 | `termux-wake-lock` + 系统设置关 Termux 电池优化 |
| 点击太快被封号 | `click_rate_per_sec` >15 | 调回 8（默认值） |

---

## 六、当前仓库结构

```
TicketRush/
├── README.md                        # 入口
├── PLAN.md                          # 这份
├── 抓包指南.md                       # 4 平台 token 抓法
├── TicketMonitoring-master/         # A 路：监控守护
│   ├── start.py                     # 自检 + 心跳 + 自适应频率
│   ├── setup_wizard.py              # 交互式配置向导
│   ├── telegram_notifier.py         # 实时推送
│   ├── email_notifier.py            # 兜底
│   ├── Monitor_MY/DM/FWD/PXQ.py     # 4 平台采集器
│   └── config.json.example
├── mobile_termux/                   # B 路：手机端
│   ├── termux_grabber.py
│   └── config.json.example
└── 截图/、参考图.jpg                 # 调按钮关键字时对照
```
