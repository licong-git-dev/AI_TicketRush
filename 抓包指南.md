# 抓包指南（A 路能否跑通的关键）

`TicketMonitoring-master` 能跑的前提是拿到每个平台的**登录态凭据**。没有这一步，监控全是 `401` 或空响应。本指南按难度从低到高列出各平台的抓包方法。

> **法律合规**：仅用于你自己的账号做监控，不得用于爬其他用户的数据。

---

## 工具准备（一次性）

### 方案 A：电脑浏览器抓微信小程序（最容易）

平台小程序几乎全部走 `wx.maoyan.com` / `mtop.damai.cn` 这类 H5 接口，走微信的 DevTools 最稳。

1. 电脑装 **Stream**（Android）或 **Charles**（macOS/Windows）。
2. 微信 PC 版打开 → 聊天框输入 `//openTools` 回车（旧方法叫 `http://debugx5.qq.com`，但微信新版小程序只能用 Stream 远程抓包）。
3. 更现代的方式：直接在电脑 **Chrome** 打开每个平台的 H5 站点登录，按 F12 进 Network，抓到的请求 header 和小程序基本一致。

### 方案 B：手机代理抓包

1. 电脑开 Charles，设置 → Proxy → Proxy Settings，记 IP 和端口（如 `192.168.1.10:8888`）。
2. 手机 Wi-Fi 设置同一个网，代理改手动填电脑 IP+端口。
3. 手机浏览器访问 `chls.pro/ssl` 下载证书并安装为系统证书（Android 7+ 需 root 或用 Android <7 的旧机；iOS 直接安装）。
4. 打开 Charles → Proxy → SSL Proxying → Enable + 通配 `*.*`，就能解 HTTPS。

### 方案 C：Android root 用小黄鸟（HttpCanary）

最简单，一切都在手机上完成，但需要 root 或 VirtualXposed。不展开。

---

## 各平台需要抓什么

### 1. 猫眼（`Monitor_MY.py`）

**需要**：1 个 `token`，填到 `config.json` → `token.my`。

**抓取**：
1. 微信扫码登录猫眼小程序。
2. 进入**任一演出详情页**。
3. 抓包工具里搜索请求 URL `wx.maoyan.com/my/odea/project/shows`。
4. 查询参数里的 `token=xxxxxxxx` 复制出来。
5. 同一账号的 token 有效期约 30 天。**开抢前 1 天必须重抓一次**。

**show_id 怎么找**：同一请求的 body 里 `projectId` 就是。或者 H5 链接 `https://i.maoyan.com/wx/album.html?id=12345` 里的 `id`。

### 2. 大麦（`Monitor_DM.py`）—— ⚠️ 坑最多

当前 `Monitor_DM.py:140-175` **硬编码了作者自己的 cookie**（`_tb_token_`、`cookie2`、`sgcookie`、`tfstk`、`munb=2218951629204` 等），这些**早已过期**，你直接跑 DM 会报 `FAIL_SYS_TOKEN_EMPTY` 或卡在风控。

**替换步骤**：
1. 电脑 Chrome 打开 `https://m.damai.cn/damai/minilogin/index.html`，扫码登录。
2. F12 → Application → Cookies → `m.damai.cn` 和 `mtop.damai.cn`，把所有 cookie 值拷下来。
3. 打开 `Monitor_DM.py`，替换 `do_request()` 里 `cookies={...}` 整块字典中的每个值为你自己的。
4. 大麦的 `sign` 是 MD5(`_m_h5_tk 前半段 + & + 时间戳 + & + appKey + & + data`)，算法在 `Monitor_DM.py:135-137` 已经实现——只要 `_m_h5_tk` 正确，签名会自动算。
5. `show_id` 从演出页 URL 拿：`https://m.damai.cn/shows/item.html?itemId=760167213815` 里的 `itemId`。

**替代方案**（推荐）：如果不想处理 cookies，**只监控猫眼+纷玩岛**（token 简单），大麦用官方 App 的"提醒我"功能兜底。

### 3. 纷玩岛（`Monitor_FWD.py`）

需要 token 的方式与猫眼类似，抓 `fwd` 域名下的请求。大多数演出无需登录就能查库存，优先级可以低一些。

### 4. 票星球（`Monitor_PXQ.py`）

同上。PXQ 出票的热门演出少，除非你的目标明确在票星球上，否则可以不配。

---

## 验证配置是否正确

写完 `config.json` 后，**先别挂全天**，用下面这条在仓库根目录跑冒烟测试：

```bash
cd TicketMonitoring-master
python3 -c "
import json, logging
logging.basicConfig(level=logging.INFO)
from Monitor_MY import MY
perform = json.load(open('config.json'))['monitor_list'][0]
m = MY(perform)
print('session_info:', m.show_info['session_info'][:2])
print('seat_info:', m.show_info['seat_info'][:2])
print('can_buy:', m.monitor())
"
```

- 如果打出 `session_info` 和 `seat_info` 列表 → token 有效，show_id 正确 ✅
- 如果报 `'NoneType' object has no attribute 'get'` → token 过期或 show_id 错 ❌
- 如果 `ConnectionError` → 网络或代理问题

---

## 为什么不建议自动下单

猫眼/大麦的 `createOrder` 接口都要 `mtop-sign` 或类似签名，而签名算法是**混淆的 JS 实时生成**，你要么：
- 逆向出 JS 算法（工作量大，平台每月改一次）
- 用 `pyexecjs` 调真实浏览器环境跑那段 JS（稳定但慢）
- 直接用浏览器（Playwright）执行 → 也就是本仓库的 C 路

所以**实战推荐**：
- A 路做监控+通知（抓包门槛低，马上跑起来）
- 真正下单交给 C 路（Playwright 复用 cookie，人机各半）
- B 路（手机 UI）作为网络最弱场景下的兜底

---

## 时间敏感度

| 事项 | 过期周期 |
|---|---|
| 猫眼 token | ~30 天 |
| 大麦 cookie 全家桶 | 7–14 天（Alibaba 风控严） |
| 纷玩岛 token | ~30 天 |
| 签名算法（JS） | 1–3 个月，但一旦改立即全废 |

**结论**：开抢前 **24 小时内** 重新抓一次 token；开抢当天提前 2 小时再冒烟测试一次。
